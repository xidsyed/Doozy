# Doozy

## Firebase Setup
To set up Firebase for this project:
1. Add your `google-services.json` file to the `app/` directory.
2. Ensure you have added your SHA fingerprints (SHA-1 and SHA-256) to your Firebase project settings to enable authentication features like Phone Auth.
