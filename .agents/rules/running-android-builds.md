---
trigger: always_on
---

When running android application builds from the terminal, always make sure to `export JAVA_HOME=/home/shiba/apps/android-studio/jbr` first before you run `./gradlew :app:assembleDebug`
